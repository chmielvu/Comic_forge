
/**
 * @license
 * SPDX-License-Identifier: Apache-2.0
*/

// This component is no longer needed as the API key for `gemini-2.5-flash-image`
// is assumed to be provided via `process.env.API_KEY` and does not
// require explicit user selection via `window.aistudio.openSelectKey()`.
// The application must not ask the user for it under any circumstances.
